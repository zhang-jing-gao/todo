import { VNode } from 'vue';
import type { TreeNode } from 'treemate';
import { MenuOption, MenuGroupOption } from './interface';
export declare function itemRenderer(tmNode: TreeNode<MenuOption, MenuGroupOption>): VNode;
