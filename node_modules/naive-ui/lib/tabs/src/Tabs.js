"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const vue_1 = require("vue");
const vueuc_1 = require("vueuc");
const lodash_1 = require("lodash");
const vooks_1 = require("vooks");
const _mixins_1 = require("../../_mixins");
const _utils_1 = require("../../_utils");
const styles_1 = require("../styles");
const interface_1 = require("./interface");
const index_cssr_1 = require("./styles/index.cssr");
const Tab_1 = require("./Tab");
const tabsProps = Object.assign(Object.assign({}, _mixins_1.useTheme.props), { value: [String, Number], defaultValue: [String, Number], type: {
        type: String,
        default: 'bar'
    }, closable: Boolean, justifyContent: String, 
    /** deprecated */
    labelSize: {
        type: String,
        validator: () => {
            if (process.env.NODE_ENV !== 'production') {
                _utils_1.warn('tabs', '`label-size` is deprecated, please use `size` instead.');
            }
            return true;
        },
        default: undefined
    }, size: {
        type: String,
        default: 'medium'
    }, tabStyle: [String, Object], paneStyle: [String, Object], addable: [Boolean, Object], tabsPadding: {
        type: Number,
        default: 0
    }, onAdd: Function, 
    // eslint-disable-next-line vue/prop-name-casing
    'onUpdate:value': [Function, Array], onUpdateValue: [Function, Array], onClose: [Function, Array], 
    // deprecated
    activeName: {
        type: [String, Number],
        validator: () => {
            if (process.env.NODE_ENV !== 'production') {
                _utils_1.warn('tabs', '`active-name` is deprecated, please use `value` instead.');
            }
            return true;
        },
        default: undefined
    }, onActiveNameChange: {
        type: [Function, Array],
        validator: () => {
            if (process.env.NODE_ENV !== 'production') {
                _utils_1.warn('tabs', '`on-active-name-change` is deprecated, please use `on-update:value` instead.');
            }
            return true;
        },
        default: undefined
    } });
exports.default = vue_1.defineComponent({
    name: 'Tabs',
    props: tabsProps,
    setup(props, { slots }) {
        var _a, _b;
        const { mergedClsPrefixRef } = _mixins_1.useConfig(props);
        const themeRef = _mixins_1.useTheme('Tabs', 'Tabs', index_cssr_1.default, styles_1.tabsLight, props, mergedClsPrefixRef);
        const tabsElRef = vue_1.ref(null);
        const barElRef = vue_1.ref(null);
        const scrollWrapperElRef = vue_1.ref(null);
        const addTabInstRef = vue_1.ref(null);
        const xScrollInstRef = vue_1.ref(null);
        const leftReachedRef = vue_1.ref(true);
        const rightReachedRef = vue_1.ref(true);
        const compitableSizeRef = vooks_1.useCompitable(props, ['labelSize', 'size']);
        const compitableValueRef = vooks_1.useCompitable(props, ['activeName', 'value']);
        const uncontrolledValueRef = vue_1.ref((_b = (_a = compitableValueRef.value) !== null && _a !== void 0 ? _a : props.defaultValue) !== null && _b !== void 0 ? _b : (slots.default
            ? _utils_1.flatten(slots.default())[0].props.name
            : null));
        const mergedValueRef = vooks_1.useMergedState(compitableValueRef, uncontrolledValueRef);
        const tabWrapperStyleRef = vue_1.computed(() => {
            if (!props.justifyContent || props.type === 'card')
                return undefined;
            return {
                display: 'flex',
                justifyContent: props.justifyContent
            };
        });
        vue_1.watch(mergedValueRef, () => {
            updateCurrentBarStyle();
        });
        function getCurrentEl() {
            var _a;
            const { value } = mergedValueRef;
            if (value === null)
                return null;
            const tabEl = (_a = tabsElRef.value) === null || _a === void 0 ? void 0 : _a.querySelector(`[data-name="${value}"]`);
            return tabEl;
        }
        function updateBarStyle(tabEl) {
            if (props.type === 'card')
                return;
            const { value: barEl } = barElRef;
            if (!barEl)
                return;
            if (tabEl) {
                const disabledClassName = `${mergedClsPrefixRef.value}-tabs-bar--disabled`;
                if (tabEl.dataset.disabled === 'true') {
                    barEl.classList.add(disabledClassName);
                }
                else {
                    barEl.classList.remove(disabledClassName);
                }
                barEl.style.left = `${tabEl.offsetLeft}px`;
                barEl.style.width = '8192px';
                barEl.style.maxWidth = `${tabEl.offsetWidth + 1}px`;
            }
        }
        function updateCurrentBarStyle() {
            if (props.type === 'card')
                return;
            const tabEl = getCurrentEl();
            if (tabEl) {
                updateBarStyle(tabEl);
            }
        }
        function handleTabClick(panelName) {
            doUpdateValue(panelName);
        }
        function doUpdateValue(panelName) {
            const { onActiveNameChange, onUpdateValue, 'onUpdate:value': _onUpdateValue } = props;
            if (onActiveNameChange) {
                _utils_1.call(onActiveNameChange, panelName);
            }
            if (onUpdateValue)
                _utils_1.call(onUpdateValue, panelName);
            if (_onUpdateValue)
                _utils_1.call(_onUpdateValue, panelName);
            uncontrolledValueRef.value = panelName;
        }
        function handleClose(panelName) {
            const { onClose } = props;
            if (onClose)
                _utils_1.call(onClose, panelName);
        }
        let firstTimeUpdatePosition = true;
        const handleNavResize = lodash_1.throttle(function handleNavResize() {
            var _a;
            const { type } = props;
            if ((type === 'line' || type === 'bar') &&
                (firstTimeUpdatePosition || props.justifyContent)) {
                const { value: barEl } = barElRef;
                if (!barEl)
                    return;
                if (!firstTimeUpdatePosition)
                    firstTimeUpdatePosition = false;
                const disableTransitionClassName = `${mergedClsPrefixRef.value}-tabs-bar--transition-disabled`;
                barEl.classList.add(disableTransitionClassName);
                updateCurrentBarStyle();
                // here we don't need to force layout after update bar style
                // since deriveScrollShadow will force layout
                deriveScrollShadow((_a = xScrollInstRef.value) === null || _a === void 0 ? void 0 : _a.$el);
                barEl.classList.remove(disableTransitionClassName);
            }
        }, 64);
        const addTabFixedRef = vue_1.ref(false);
        function _handleTabsResize(entry) {
            var _a;
            const { target, contentRect: { width } } = entry;
            // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
            const containerWidth = target.parentElement.offsetWidth;
            if (!addTabFixedRef.value) {
                if (containerWidth < width) {
                    addTabFixedRef.value = true;
                }
            }
            else {
                const { value: addTabInst } = addTabInstRef;
                if (!addTabInst)
                    return;
                if (containerWidth - width > addTabInst.$el.offsetWidth) {
                    addTabFixedRef.value = false;
                }
            }
            deriveScrollShadow((_a = xScrollInstRef.value) === null || _a === void 0 ? void 0 : _a.$el);
        }
        const handleTabsResize = lodash_1.throttle(_handleTabsResize, 64);
        function handleAdd() {
            const { onAdd } = props;
            if (onAdd)
                onAdd();
            void vue_1.nextTick(() => {
                const currentEl = getCurrentEl();
                const { value: xScrollInst } = xScrollInstRef;
                if (!currentEl || !xScrollInst)
                    return;
                xScrollInst.scrollTo({
                    left: currentEl.offsetLeft,
                    top: 0,
                    behavior: 'smooth'
                });
            });
        }
        function deriveScrollShadow(el) {
            if (!el)
                return;
            const { scrollLeft, scrollWidth, offsetWidth } = el;
            leftReachedRef.value = scrollLeft <= 0;
            rightReachedRef.value = scrollLeft + offsetWidth >= scrollWidth;
        }
        const handleScroll = lodash_1.throttle((e) => {
            deriveScrollShadow(e.target);
        }, 64);
        vue_1.provide(interface_1.tabsInjectionKey, {
            tabStyleRef: vue_1.toRef(props, 'tabStyle'),
            paneStyleRef: vue_1.toRef(props, 'paneStyle'),
            mergedClsPrefixRef,
            typeRef: vue_1.toRef(props, 'type'),
            closableRef: vue_1.toRef(props, 'closable'),
            valueRef: mergedValueRef,
            handleTabClick,
            handleClose,
            handleAdd
        });
        vooks_1.onFontsReady(() => {
            updateCurrentBarStyle();
        });
        // avoid useless rerender
        vue_1.watchEffect(() => {
            const { value: el } = scrollWrapperElRef;
            if (!el)
                return;
            const { value: clsPrefix } = mergedClsPrefixRef;
            const shadowBeforeClass = `${clsPrefix}-tabs-nav-scroll-wrapper--shadow-before`;
            const shadowAfterClass = `${clsPrefix}-tabs-nav-scroll-wrapper--shadow-after`;
            if (leftReachedRef.value) {
                el.classList.remove(shadowBeforeClass);
            }
            else {
                el.classList.add(shadowBeforeClass);
            }
            if (rightReachedRef.value) {
                el.classList.remove(shadowAfterClass);
            }
            else {
                el.classList.add(shadowAfterClass);
            }
        });
        return {
            mergedClsPrefix: mergedClsPrefixRef,
            mergedValue: mergedValueRef,
            tabsElRef,
            barElRef,
            addTabInstRef,
            xScrollInstRef,
            scrollWrapperElRef,
            addTabFixed: addTabFixedRef,
            tabWrapperStyle: tabWrapperStyleRef,
            handleNavResize,
            mergedSize: compitableSizeRef,
            handleScroll,
            handleTabsResize,
            cssVars: vue_1.computed(() => {
                const { value: size } = compitableSizeRef;
                const { type } = props;
                const typeSuffix = type === 'card' ? 'Card' : type === 'bar' ? 'Bar' : 'Line';
                const sizeType = `${size}${typeSuffix}`;
                const { self: { barColor, closeColor, closeColorHover, closeColorPressed, tabColor, tabBorderColor, paneTextColor, tabFontWeight, tabBorderRadius, tabFontWeightActive, [_utils_1.createKey('panePadding', size)]: panePadding, [_utils_1.createKey('tabPadding', sizeType)]: tabPadding, [_utils_1.createKey('tabGap', sizeType)]: tabGap, [_utils_1.createKey('tabTextColor', type)]: tabTextColor, [_utils_1.createKey('tabTextColorActive', type)]: tabTextColorActive, [_utils_1.createKey('tabTextColorHover', type)]: tabTextColorHover, [_utils_1.createKey('tabTextColorDisabled', type)]: tabTextColorDisabled, [_utils_1.createKey('tabFontSize', size)]: tabFontSize }, common: { cubicBezierEaseInOut } } = themeRef.value;
                return {
                    '--bezier': cubicBezierEaseInOut,
                    '--bar-color': barColor,
                    '--tab-font-size': tabFontSize,
                    '--tab-text-color': tabTextColor,
                    '--tab-text-color-active': tabTextColorActive,
                    '--tab-text-color-disabled': tabTextColorDisabled,
                    '--tab-text-color-hover': tabTextColorHover,
                    '--pane-text-color': paneTextColor,
                    '--tab-border-color': tabBorderColor,
                    '--tab-border-radius': tabBorderRadius,
                    '--close-color': closeColor,
                    '--close-color-hover': closeColorHover,
                    '--close-color-pressed': closeColorPressed,
                    '--tab-color': tabColor,
                    '--tab-font-weight': tabFontWeight,
                    '--tab-font-weight-active': tabFontWeightActive,
                    '--tab-padding': tabPadding,
                    '--tab-gap': tabGap,
                    '--pane-padding': panePadding
                };
            })
        };
    },
    render() {
        const { mergedClsPrefix, type, addTabFixed, addable, mergedSize, $slots: { default: defaultSlot, prefix: prefixSlot, suffix: suffixSlot } } = this;
        const children = defaultSlot
            ? _utils_1.flatten(defaultSlot()).filter((v) => {
                return v.type.__TAB_PANE__ === true;
            })
            : [];
        const prefix = prefixSlot ? prefixSlot() : null;
        const suffix = suffixSlot ? suffixSlot() : null;
        const isCard = type === 'card';
        const mergedJustifyContent = !isCard && this.justifyContent;
        return (vue_1.h("div", { class: [
                `${mergedClsPrefix}-tabs`,
                `${mergedClsPrefix}-tabs--${type}-type`,
                `${mergedClsPrefix}-tabs--${mergedSize}-size`,
                mergedJustifyContent && `${mergedClsPrefix}-tabs--flex`
            ], style: this.cssVars },
            vue_1.h("div", { class: `${mergedClsPrefix}-tabs-nav` },
                prefix ? (vue_1.h("div", { class: `${mergedClsPrefix}-tabs-nav__prefix` }, prefix)) : null,
                vue_1.h(vueuc_1.VResizeObserver, { onResize: this.handleNavResize }, {
                    default: () => (vue_1.h("div", { class: `${mergedClsPrefix}-tabs-nav-scroll-wrapper`, ref: "scrollWrapperElRef" },
                        vue_1.h(vueuc_1.VXScroll, { ref: "xScrollInstRef", onScroll: this.handleScroll }, {
                            default: () => {
                                const rawWrappedTabs = (vue_1.h("div", { style: this.tabWrapperStyle, class: `${mergedClsPrefix}-tabs-wrapper` },
                                    mergedJustifyContent ? null : (vue_1.h("div", { class: `${mergedClsPrefix}-tabs-scroll-padding`, style: { width: `${this.tabsPadding}px` } })),
                                    children.map((tabPaneVNode, index) => {
                                        return (vue_1.h(Tab_1.default, Object.assign({}, tabPaneVNode.props, { leftPadded: index !== 0 && !mergedJustifyContent }), tabPaneVNode.children
                                            ? {
                                                default: tabPaneVNode.children.tab
                                            }
                                            : undefined));
                                    }),
                                    !addTabFixed && addable && isCard
                                        ? createAddTag(addable, children.length !== 0)
                                        : null,
                                    mergedJustifyContent ? null : (vue_1.h("div", { class: `${mergedClsPrefix}-tabs-scroll-padding`, style: { width: `${this.tabsPadding}px` } }))));
                                let wrappedTabs = rawWrappedTabs;
                                if (isCard && addable) {
                                    wrappedTabs = (vue_1.h(vueuc_1.VResizeObserver, { onResize: this.handleTabsResize }, {
                                        default: () => rawWrappedTabs
                                    }));
                                }
                                return (vue_1.h("div", { ref: "tabsElRef", class: `${mergedClsPrefix}-tabs-nav-scroll-content` },
                                    wrappedTabs,
                                    isCard ? (vue_1.h("div", { class: `${mergedClsPrefix}-tabs-pad` })) : null,
                                    isCard ? null : (vue_1.h("div", { ref: "barElRef", class: `${mergedClsPrefix}-tabs-bar` }))));
                            }
                        })))
                }),
                addTabFixed && addable && isCard
                    ? createAddTag(addable, true)
                    : null,
                suffix ? (vue_1.h("div", { class: `${mergedClsPrefix}-tabs-nav__suffix` }, suffix)) : null),
            filterMapTabPanes(children, this.mergedValue)));
    }
});
function filterMapTabPanes(tabPaneVNodes, value) {
    const children = [];
    tabPaneVNodes.forEach((vNode) => {
        const { name, displayDirective, 'display-directive': _displayDirective } = vNode.props;
        const useVShow = displayDirective === 'show' || _displayDirective === 'show';
        const show = value === name;
        if (vNode.key !== undefined) {
            vNode.key = name;
        }
        if (useVShow) {
            children.push(vue_1.withDirectives(vNode, [[vue_1.vShow, show]]));
        }
        else if (show) {
            children.push(vNode);
        }
    });
    return children;
}
function createAddTag(addable, leftPadded) {
    return (vue_1.h(Tab_1.default, { ref: "addTabInstRef", key: "__addable", name: "__addable", addable: true, leftPadded: leftPadded, disabled: typeof addable === 'object' && addable.disabled }));
}
