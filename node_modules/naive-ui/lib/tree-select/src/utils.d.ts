import { SelectBaseOption } from '../../select/src/interface';
import { Key } from '../../tree/src/interface';
import { TreeSelectOption } from './interface';
export declare function treeOption2SelectOption(treeOpt: TreeSelectOption): SelectBaseOption;
export declare function filterTree(tree: TreeSelectOption[], filter: (pattern: string, v: TreeSelectOption) => boolean, pattern: string): {
    filteredTree: TreeSelectOption[];
    expandedKeys: Key[];
    highlightKeySet: Set<Key>;
};
