import { Placement, Rect, TransformOrigin } from './interface';
export declare function getProperPlacementOfFollower(placement: Placement, targetRect: Rect, followerRect: Rect, flip: boolean, overlap: boolean): Placement;
export declare function getProperTransformOrigin(placement: Placement, overlap: boolean): TransformOrigin;
interface PlacementOffset {
    top: string;
    left: string;
    transform: string;
}
export declare function getOffset(placement: Placement, offsetRect: Rect, targetRect: Rect, overlap: boolean): PlacementOffset;
export {};
